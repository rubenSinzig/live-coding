// .lenght .length -1
// !!!!! store deleted elements in a VAR !!!!!
// end .push() .pop()
// start .unshift() .shift()
// .indexOf()
// .splice(indexOf, how many) rm item
// for (let i = 0; i < array.length; i++) {}
// .split() str => arr
// .join() arr => str
// .concat() merge two or more arrays
// .slice(indexOf, end indexOf) shallow copy
// ################## NEW ##################
//  const new Array(), new String(), new Number()
// .of() Array.of(1, 44, 55, 6)
// .sort() sort the elements in a Array
// .reverse() first element is last and last element is first
//  Array.isArray() ([])
// .lastIndexOf() search from backwards ("what we search") -1 = not found
// .fill() (what, from, to) overwrite the existing elements
/*
    do {
    
    } while (counter < 10); 
    counter outside the loop
*/
/*  
    while (counter < 10){

    };
    counter outside the loop
*/
